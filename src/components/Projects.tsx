export default function Projects({id}: {id:string}){
    
    
    
    return(
        <section className="h-screen snap-center bg-red-500 flex justify-center items-center" id={id}>
            Projects
        </section>
    )
}